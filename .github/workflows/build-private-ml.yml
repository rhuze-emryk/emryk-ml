---
name: Build private-ml variant
on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'Containerfile.private-ml'
      - 'build_files/private-ml-install.sh'
      - 'build_files/verify-payload-rpm-owned.sh'
      - '.github/workflows/build-private-ml.yml'
  # Variant CI on PRs that touch variant-relevant files. The base image runs
  # on every PR via build.yml; this catches variant-only regressions (e.g.
  # the non-RPM payload guard failing against private-ml-install.sh output)
  # before merge, instead of post-merge via workflow_run.
  pull_request:
    branches:
      - main
    paths:
      - 'Containerfile.private-ml'
      - 'build_files/private-ml-install.sh'
      - 'build_files/verify-payload-rpm-owned.sh'
      - '.github/workflows/build-private-ml.yml'
  # Re-build the variant whenever the base image is re-built so they stay in sync.
  workflow_run:
    workflows: ["Build container image"]
    types: [completed]
    branches: [main]

env:
  IMAGE_DESC: "Emryk ML with Mullvad VPN daemon and Unsloth Studio preconfigured. Variant of emryk-ml:latest."
  IMAGE_KEYWORDS: "bootc,ublue,kinoite,nvidia,machine-learning,fedora,kde,cloud-workstation,unsloth,mullvad,vpn,llm"
  IMAGE_LOGO_URL: "https://avatars.githubusercontent.com/u/120078124?s=200&v=4"
  IMAGE_NAME: "${{ github.event.repository.name }}"
  IMAGE_REGISTRY: "ghcr.io/${{ github.repository_owner }}"
  DEFAULT_TAG: "latest-private-ml"
  # Pinned for reproducibility; bumped by renovate (SECURITY-TODO #14).
  GRYPE_VERSION: "v0.112.0"
  SYFT_VERSION: "v1.44.0"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref || github.run_id }}
  cancel-in-progress: true

jobs:
  build_push:
    name: Build and push private-ml variant
    runs-on: ubuntu-24.04
    # When chained off the base build via workflow_run, skip on failed/cancelled
    # base runs. Other triggers always run.
    if: github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success'

    permissions:
      contents: read
      packages: write
      id-token: write
      # SECURITY-TODO #8: needed for SLSA provenance + SBOM attestations.
      attestations: write

    steps:
      - name: Prepare environment
        run: |
          echo "IMAGE_REGISTRY=${IMAGE_REGISTRY,,}" >> ${GITHUB_ENV}
          echo "IMAGE_NAME=${IMAGE_NAME,,}" >> ${GITHUB_ENV}

      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Lint shell scripts
        run: shellcheck build_files/private-ml-install.sh

      - name: Maximize build space
        uses: ublue-os/remove-unwanted-software@695eb75bc387dbcd9685a8e72d23439d8686cba6 # master as of 2025-10-10 (post-v9, untagged)

      - name: Get current date
        id: date
        run: echo "date=$(date -u +%Y\-%m\-%d\T%H\:%M\:%S\Z)" >> $GITHUB_OUTPUT

      - name: Image Metadata
        uses: docker/metadata-action@030e881283bb7a6894de51c315a6bfe6a94e05cf # v6.0.0
        id: metadata
        with:
          tags: |
            type=raw,value=${{ env.DEFAULT_TAG }}
            type=raw,value=${{ env.DEFAULT_TAG }}.{{date 'YYYYMMDD'}}
          labels: |
            io.artifacthub.package.readme-url=https://raw.githubusercontent.com/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}/${{ github.sha }}/README.md
            org.opencontainers.image.created=${{ steps.date.outputs.date }}
            org.opencontainers.image.description=${{ env.IMAGE_DESC }}
            org.opencontainers.image.documentation=https://raw.githubusercontent.com/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}/${{ github.sha }}/README.md
            org.opencontainers.image.source=https://github.com/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}/blob/${{ github.sha }}/Containerfile.private-ml
            org.opencontainers.image.title=${{ env.IMAGE_NAME }}
            org.opencontainers.image.url=https://github.com/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}/tree/${{ github.sha }}
            org.opencontainers.image.vendor=${{ github.repository_owner }}
            org.opencontainers.image.version=${{ env.DEFAULT_TAG }}.{{date 'YYYYMMDD'}}
            io.artifacthub.package.deprecated=false
            io.artifacthub.package.keywords=${{ env.IMAGE_KEYWORDS }}
            io.artifacthub.package.license=Apache-2.0
            io.artifacthub.package.logo-url=${{ env.IMAGE_LOGO_URL }}
            io.artifacthub.package.prerelease=false
            containers.bootc=1
          sep-tags: " "
          sep-annotations: " "

      - name: Build Image
        id: build_image
        uses: redhat-actions/buildah-build@061ffd31bb5c3f8733a6f9bbdf00a9fb02eb708c # main as of 2026-04-09 (Node 24)
        with:
          containerfiles: |
            ./Containerfile.private-ml
          image: ${{ env.IMAGE_NAME }}
          tags: ${{ steps.metadata.outputs.tags }}
          labels: ${{ steps.metadata.outputs.labels }}
          oci: false

      # SECURITY-TODO #3 + #8: extract RPM database, generate CycloneDX SBOM
      # via syft, and CVE-scan via grype against the SBOM. See the matching
      # comment in build.yml for the RPM-only coverage tradeoff.
      - name: Extract RPM database from built image
        run: |
          # cp -RL dereferences symlinks (the rpm/ path is a symlink on this
          # image); see build.yml for the full rationale.
          buildah unshare bash -ec '
            ctr=$(buildah from "localhost/${IMAGE_NAME}:${DEFAULT_TAG}")
            root=$(buildah mount "$ctr")
            mkdir -p ./sbom-input/usr/lib/sysimage/rpm
            cp -RL "$root/usr/lib/sysimage/rpm/." ./sbom-input/usr/lib/sysimage/rpm/
            buildah umount "$ctr"
            buildah rm "$ctr"
          '
          du -sh ./sbom-input
          size_kb=$(du -sk ./sbom-input | cut -f1)
          if [ "$size_kb" -lt 10000 ]; then
            echo "::error::sbom-input is only ${size_kb}KB — RPM DB extract failed."
            exit 1
          fi

      - name: Install Syft
        run: |
          curl -fsSL "https://github.com/anchore/syft/releases/download/${SYFT_VERSION}/syft_${SYFT_VERSION#v}_linux_amd64.tar.gz" \
            | sudo tar -xz -C /usr/local/bin syft
          syft version

      - name: Generate SBOM (CycloneDX)
        run: |
          syft scan dir:./sbom-input -o cyclonedx-json=sbom.cdx.json
          {
            echo "## SBOM"
            echo ""
            echo "Generated \`sbom.cdx.json\` ($(wc -c < sbom.cdx.json) bytes,"
            echo "$(jq -r '.components | length' sbom.cdx.json) components) from RPM database."
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Install Grype
        run: |
          curl -fsSL "https://github.com/anchore/grype/releases/download/${GRYPE_VERSION}/grype_${GRYPE_VERSION#v}_linux_amd64.tar.gz" \
            | sudo tar -xz -C /usr/local/bin grype
          grype version

      - name: Scan SBOM with Grype (report-only)
        continue-on-error: true
        run: |
          grype "sbom:./sbom.cdx.json" --output table > grype-report.txt
          {
            echo "## Grype vulnerability scan — \`${IMAGE_NAME}:${DEFAULT_TAG}\`"
            echo ""
            echo '```'
            cat grype-report.txt
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Login to GitHub Container Registry
        uses: docker/login-action@4907a6ddec9925e35a0a9e82d7399ccc52663121 # v4.1.0
        if: github.event_name != 'pull_request' && github.ref == format('refs/heads/{0}', github.event.repository.default_branch)
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # Inline replacement for redhat-actions/push-to-registry — see the
      # matching block in build.yml for rationale.
      - name: Push To GHCR
        id: push
        if: github.event_name != 'pull_request' && github.ref == format('refs/heads/{0}', github.event.repository.default_branch)
        run: |
          IMAGE_FULL="${IMAGE_REGISTRY}/${IMAGE_NAME}"
          digest=""
          for tag in ${{ steps.metadata.outputs.tags }}; do
            if [ -z "$digest" ]; then
              buildah push --digestfile=./push-digest.txt \
                "localhost/${IMAGE_NAME}:${tag}" \
                "docker://${IMAGE_FULL}:${tag}"
              digest=$(cat ./push-digest.txt)
            else
              buildah push \
                "localhost/${IMAGE_NAME}:${tag}" \
                "docker://${IMAGE_FULL}:${tag}"
            fi
          done
          echo "digest=${digest}" >> "$GITHUB_OUTPUT"
          echo "Pushed ${IMAGE_FULL} @ ${digest}"

      - name: Install Cosign
        uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6 # v4.1.2
        if: github.event_name != 'pull_request' && github.ref == format('refs/heads/{0}', github.event.repository.default_branch)
        with:
          cosign-release: 'v2.6.1'

      - name: Sign container image
        if: github.event_name != 'pull_request' && github.ref == format('refs/heads/{0}', github.event.repository.default_branch)
        run: |
          IMAGE_FULL="${{ env.IMAGE_REGISTRY }}/${{ env.IMAGE_NAME }}"
          for tag in ${{ steps.metadata.outputs.tags }}; do
            cosign sign -y --key env://COSIGN_PRIVATE_KEY $IMAGE_FULL:$tag
          done
        env:
          TAGS: ${{ steps.push.outputs.digest }}
          COSIGN_EXPERIMENTAL: false
          COSIGN_PRIVATE_KEY: ${{ secrets.SIGNING_SECRET }}

      # SECURITY-TODO #8: SLSA build provenance + CycloneDX SBOM attestations.
      # Both signed via Sigstore using the workflow's short-lived OIDC token
      # and pushed as OCI referrers — see build.yml for the full rationale.
      - name: Attest build provenance
        uses: actions/attest-build-provenance@a2bbfa25375fe432b6a289bc6b6cd05ecd0c4c32 # v4.1.0
        if: github.event_name != 'pull_request' && github.ref == format('refs/heads/{0}', github.event.repository.default_branch)
        with:
          subject-name: ${{ env.IMAGE_REGISTRY }}/${{ env.IMAGE_NAME }}
          subject-digest: ${{ steps.push.outputs.digest }}
          push-to-registry: true

      - name: Attest SBOM
        uses: actions/attest@59d89421af93a897026c735860bf21b6eb4f7b26 # v4.1.0
        if: github.event_name != 'pull_request' && github.ref == format('refs/heads/{0}', github.event.repository.default_branch)
        with:
          subject-name: ${{ env.IMAGE_REGISTRY }}/${{ env.IMAGE_NAME }}
          subject-digest: ${{ steps.push.outputs.digest }}
          sbom-path: sbom.cdx.json
          push-to-registry: true
