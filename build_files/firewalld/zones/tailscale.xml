<?xml version="1.0" encoding="utf-8"?>
<zone target="ACCEPT">
  <short>Tailscale</short>
  <description>Tailscale tailnet — trusted private network. All traffic from the tailscale0 interface is accepted, so the management plane (SSH and Cockpit) is reachable over the tailnet only.</description>
  <interface name="tailscale0"/>
</zone>
