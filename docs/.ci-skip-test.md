<!--
Throwaway file to validate that docs-only PRs skip the image build while the
required "Build and push image" check still reports green. This PR will be
closed without merging; this file should never reach main.
-->
# CI skip validation

Temporary — safe to delete.
